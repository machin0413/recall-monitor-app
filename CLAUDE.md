# 開発メモ

国土交通省のリコール情報から、自分の車が対象かを調べる iOS アプリ。
**アプリが国交省の API を直接呼ぶ。配信サーバーは持たない。**

このファイルは次にコードを触る人（人間でもエージェントでも）向け。
使い方や運用は README を見てほしい。ここには**踏んではいけない罠**と
**破ってはいけない不変条件**だけを書く。

---

## ビルドとテスト

```bash
open ios/RecallMonitor.xcodeproj      # そのまま実行できる
```

`.xcodeproj` はコミットしてある。フォルダ同期方式（`PBXFileSystemSynchronizedRootGroup`）
なので、`ios/RecallMonitor/` や `ios/RecallMonitorTests/` にファイルを足せば
自動でターゲットに入る。プロジェクト生成ツールは要らない。

```bash
cd ios && xcodebuild test -project RecallMonitor.xcodeproj -scheme RecallMonitor \
  -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGNING_ALLOWED=NO

python3 scripts/check_api.py          # 実APIのカナリア（ネットワークが要る）
```

---

## 踏んではいけない罠

この API は公開仕様ではない。以下はすべて**実際に叩いて確かめた挙動**で、
そのほとんどは一度踏んで痛い目を見たもの。

### エンドポイントはサイトルート直下

```
正: https://renrakuda.mlit.go.jp/mt/mt-estraier.cgi
誤: https://renrakuda.mlit.go.jp/renrakuda/mt-search.cgi
```

**誤ったパスは 404 ではなく HTTP 200 + 本文 0 バイトを返す。** エラーにならない
ので気づけない。実際この誤りのまま、架空のサンプルデータが数日間公開され続けた。
「データが取れない」ときは、ステータスではなく**本文の長さ**を見ること。

### クエリの形

```
blog_id=4
class=recalldatacar
notification_date=0000/00/00 9999/12/31   ← from/to ではなく空白区切りの単一パラメータ
order_by=recall_data_car_mlit_notification_date
order_condition=STRD                       ← desc ではない
model_name=ZVW50                           ← 型式。部分一致
offset=1（1始まり） / limit（最大1000）
```

`selCarTp` は画面遷移用で、API には渡さない。

### レスポンスの癖

- JSON の前後に空白・改行が入る（Movable Type のテンプレート出力）
- **配列やオブジェクトの末尾に余分なカンマが付く。** 素の `JSONDecoder` は失敗する。
  除去は `RecallAPIClient.stripTrailingCommas` が担当。正規表現で一括置換すると
  本文に「,]」を含む届出を壊すので、文字列リテラルの内外を見分けること
- **`typeList` には 32 件までしか入らず、超過分が `typeList1`〜`typeList60` に
  分割される。** 実データの 7% が該当し、`typeList` だけ読むと大量に取りこぼす。
  対象車種の多い大規模リコールほど分割されるので、いちばん該当者が多い届出で
  「該当なし」と誤答する。`chassis_list1`〜`5` も同様
- メーカー名は届出側になく、型リストの `recall_type_data_car_mlit_car_name_code` にしかない
- 車台番号の終端のキーは `mst_chassis_car_mlit_chassis_to_to`（API 側の誤記だが直せない）
- **18時〜翌8時頃はデータ更新帯**で、繋がりにくい

### model_name の癖

- **小文字・全角では 0 件になる。** 送信前に `RecallMatcher.searchQuery(for:)` を通す
- **部分一致で絞り込む。** パラメータ名を間違えると絞り込みが無視されて全件返るため、
  「0 件でないこと」だけを見る検査では壊れを検出できない（カナリアで一度素通りした）

---

## 破ってはいけない不変条件

照合はこのアプリの中核で、**実際に 3 回バグを出している。すべて見逃し方向**
（対象なのに「対象外」と答える）だった。以下を崩さないこと。

### 1. 排ガス記号の有無を双方向に吸収する

規制記号（ハイフンより前の 1〜3 文字。`DAA`, `BC`, `7CF`）は**届出によって
有ったり無かったりする**。実データのユニーク型式 22,225 種のうち 14% が記号を持たず、
同じ車体が `BC-ZRT10A` と `ZRT10A` の両方で登録されている例が 64 件ある。

一方向の部分一致にすると、車検証どおり `BC-ZRT10A` と入力した人に対して
`ZRT10A` で登録された 1999 年の届出が見えなくなる。**古い未改修リコールこそ
危険なのに**、それが消える。

キー集合の積で判定すること。`BC-ZRT10A` → `{BCZRT10A, ZRT10A}`。
API へは記号を落とした本体を投げて網を広く張り、絞り込みは端末側で行う。

部分一致に戻してはいけない。`GG` が `XXX-GGYY` に当たるようになり、
無関係な車が「対象の可能性あり」と並ぶ。

### 2. 車台番号は区切りの有無どちらも受ける

`ZVW506000100` のように区切りなしで入力される。**型式部分にも数字が含まれる**ため
「末尾の数字列」で切ると `ZVW` / `506000100` と誤る。届出側が持つ範囲の
プレフィックス（`ZVW50`）を起点に削ること。

日本語キーボードでは「-」が長音符「ー」(U+30FC) になる。全角もそのまま来る。

### 3. 不連続な複数範囲は範囲ごとに判定する

1 つの型式に離れた範囲が入る（`ZVW50-6000001〜6118168` と `ZVW50-8000001〜8077900`）。
まとめて最小〜最大で見ると、隙間の `ZVW50-7000000` を誤って対象にしてしまう。

### 4. 判断がつかないものは「対象外」にしない

安全に関わる情報なので、取りこぼしより過検出を選ぶ。

- 車台番号が空、または読み取れない → `.possible`
- 届出側の範囲が数値比較できない（輸入車のシリアル番号。対象範囲の 22%）→ `.possible`
- 明らかに別のプレフィックスだと分かるときだけ `.none`

**「対象です」と断定しない。** 国交省サイト自身が、車台番号が範囲内でも仕様
（変速機・ターボの有無等）により対象外の場合があると注意している。
表示は「対象の可能性が高い」まで。

### 5. 「該当なし」を「対象外」と読ませない

API が候補を返したのに 1 件も一致しなかった場合は、入力が車検証と違う可能性が高い。
「これは対象外という意味ではありません」と明示すること。

### 6. 結果の打ち切りを黙って捨てない

上限に達したら利用者に伝える。通知の抜けに直結する。

---

## 設定と復旧

エンドポイントとパラメータ名は**リポジトリ直下の `config.json`** にある。
アプリは起動時に `raw.githubusercontent.com` から取得し、取れなければ
キャッシュ、それも無ければ `RemoteConfig.builtIn` で動く。

仕様が変わったら **config.json を直して main に push すれば全端末が復旧する**
（App Store 更新は不要）。`notice` に文言を入れると検索画面に周知が出る。

注意点が 2 つある。

- **反映まで 5〜6 分かかる。** raw の CDN が `max-age=300` でキャッシュしており、
  クライアントから `no-cache` を送っても無視される。**push 直後に反映されて
  いないのが正常。** 慌てて何度も直さないこと
- **設定の取得はキャッシュを迂回している**（`reloadIgnoringLocalAndRemoteCacheData`）。
  条件付き GET に戻すと端末側にもう一段キャッシュが乗り、CDN が新しくなっても
  古い設定を掴み続ける。取得は成功して取得時刻も更新されるのに中身だけ古い、
  という気づきにくい壊れ方をする

`config.json` を書き換えたら `RemoteConfig.builtIn` も同じ値に揃えること。
ずれているとカナリアが落ちる。

**吸収できないのはレスポンスのフィールド名の変更。** デコーダの修正が要るので
アプリ更新が必要になる。config.json が扱うのは「どこへ何を送るか」だけ。

---

## テストの方針

役割を 2 つに分けてある。**混ぜないこと。**

| | 担当 | 実行 |
|---|---|---|
| `ios/RecallMonitorTests/` | 自分たちのロジックが正しいか | 毎 PR・ネットワーク不要 |
| `scripts/check_api.py` | 外界が変わっていないか | 毎日・実 API |

ユニットテストは `Fixtures/` に記録した実応答だけで走る。新しい届出のパターンで
壊れたら、その応答を固定ファイルとして足すこと。

**見つけた不具合は必ず回帰テストにしてから閉じる。** ここに挙げた不変条件は
すべて対応するテストがある。検出できることも、修正を意図的に戻して確認済み。

---

## 確認できていないこと

- **実機で動かしていない。** 検証はすべて iOS シミュレータ（iPhone 17 / iOS 26.5）
- **バックグラウンド更新が実際に発火するところを見ていない。** `BGAppRefreshTask` は
  OS 判断で走るため、シミュレータでの再現が難しい
- **通知が画面に出るところを見ていない。** 発火経路（登録直後は鳴らさない、
  複数台が同じリコールに該当したら別々に鳴る）は UserDefaults の記録で確認しただけ
- **利用規約を読んでいない。** 全端末が国交省の非公開エンドポイントを直接叩く構成
  なので、公開前に確認が要る
